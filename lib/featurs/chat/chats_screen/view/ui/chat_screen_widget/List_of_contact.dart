import 'package:flutter/material.dart';

class ListOfContact extends StatelessWidget {
  const ListOfContact({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView.builder(itemBuilder: (c, i) {});
  }
}
